# Codex Subscription Router 项目指引

先核对当前分支、实际平台和 [兼容矩阵](docs/COMPATIBILITY.md)。默认分支的安装入口是 macOS `install.sh`；Windows 支持须以当前分支确实存在并记录的脚本为准，不能从其他功能分支猜测命令或支持状态。

- 修改路由前读 [架构](docs/ARCHITECTURE.md)；涉及账号、登录、凭据、插件会话或子进程边界时读 [安全模型](docs/SECURITY-MODEL.md)。
- 官方应用只作构建输入，不修改原安装。每账号的状态隔离、会话归属与后续消息黏着必须保持；不把真实账号状态、Token 或官方二进制提交仓库。
- 修改用户配置或安装副本前，先做完整、可读、带时间戳的备份并记录回滚。测试使用独立应用副本和数据目录，不停止用户正在使用的官方应用或占用其服务端口。
- 路由或协议修改运行相关 Go 包测试；共享 mux/state/backend 行为扩大到 `go test ./...` 和 `go vet ./...`。JS、Python、shell 修改选择 package.json 的对应检查。发布前运行完整适用检查。
- 安装、签名、补丁锚点、原生辅助程序或交付物变更时，执行 [Smoke test](docs/SMOKE-TEST.md) 与 [发布流程](docs/RELEASING.md) 中适用的真实平台验收，记录官方版本、构建和产物证据。其他平台的通过结果不能代替目标平台验证。
- 普通文档修改检查 diff 与引用；不为此重装应用、进行账号登录或消耗重置额度。发布流程中的动作仍需对应用户授权；Smoke test 中消费重置额度的步骤需单次明确授权。
